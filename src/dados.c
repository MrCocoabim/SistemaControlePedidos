#include "dados.h"


struct Cliente listaClientes[MAX_CLIENTES];
int numClientes = 0;

struct Produto listaProdutos[MAX_PRODUTOS];
int numProdutos = 0;

struct Pedido listaPedidos[MAX_PEDIDOS];
int numPedidos = 0;

struct ItemPedido listaItensPedido[MAX_ITENS_PEDIDO];
int numItensPedido = 0;
